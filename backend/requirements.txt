fastapi>=0.115.6,<1.0
uvicorn[standard]>=0.34.0,<1.0
aiosqlite>=0.20.0,<1.0
aiohttp>=3.13.0,<4.0
psutil>=7.0.0,<8.0
watchdog>=6.0.0,<7.0
pydantic>=2.10.0,<3.0
python-multipart>=0.0.20,<1.0

# These ranges intentionally avoid old pins that do not ship wheels for
# Python 3.13/3.14 on every supported platform. Keep lower bounds high enough
# for modern Python, but leave patch/minor updates open for security fixes.
